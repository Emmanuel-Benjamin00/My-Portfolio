import React from 'react'

export const metadata = {
    title: "Projects",
    description: "About | Emmanuel Benjamin",
  };

const layout = ({children}) => {
  return (   
    <>
    {children}
    </> 
  )
}

export default layout